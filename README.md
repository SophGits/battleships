# Battleships 1-player CLI

## To play
Uncover three vessels hiding in the Battleships grid by guessing their coordinates.

## To run
* `npm install` to install Jasmine-node, then `npm test` to run tests
* `node main.js` to start playing
